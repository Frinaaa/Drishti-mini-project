import React from 'react';
import { View } from 'react-native';

// This is a dummy screen that will never be displayed.
// Its purpose is to give the tab bar button a route to point to.
export default function NgoManagementRedirectScreen() {
  return <View />;
}